- need a save plan tool for the planner to write the plan to a specialized directory (probably in the root directory), then return the path so that the executor can find,
read, and implement the plan. This tool should be under a special permissions tag (not just write)
- 